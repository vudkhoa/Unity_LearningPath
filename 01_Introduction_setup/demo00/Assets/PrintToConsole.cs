using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Vu Khoa --> Dev Game");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
